# dds-bridge


## Build the application
./gradlew clean build


## Run the Application
./gradlew run


## Build the Dockerfile
docker build -t dds-bridge .


## Run Container
docker run --rm dds-bridge